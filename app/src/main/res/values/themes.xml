<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Cold start: no branded icon flash; hands off to Compose immediately -->
    <style name="Theme.Mindora" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon_empty</item>
        <item name="postSplashScreenTheme">@style/Theme.Mindora.Main</item>
    </style>

    <style name="Theme.Mindora.Main" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
