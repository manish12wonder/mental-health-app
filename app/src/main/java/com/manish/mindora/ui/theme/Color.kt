package com.manish.mindora.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Mindora brand palette — soft greens (growth), soft blues (calm), warm off-white surfaces.
 */
object MindoraPalette {
    val MintGreen = Color(0xFFA8E6CF)
    val PastelGreen = Color(0xFFB7F0D8)
    val SkyBlue = Color(0xFFA0D8EF)
    val BabyBlue = Color(0xFFB3E5FC)
    val BackgroundGreenWhite = Color(0xFFF7FBFA)
    val SurfaceBluishWhite = Color(0xFFF5F9FF)
    val TextPrimary = Color(0xFF2E3A3A)
    val TextSecondary = Color(0xFF6B7C7C)
    val Divider = Color(0xFFE0E7E7)
}

/** Bar chart colors — tuned to sit well with mint / sky surfaces. */
object MindoraMoodChart {
    val Happy = Color(0xFF5CB894)
    val Neutral = Color(0xFF8FA9A9)
    val Sad = Color(0xFF5B8FC9)
    /** Used for Angry / stressed / anxious in charts and pickers. */
    val Angry = Color(0xFFE57373)
    val Empty = Color(0xFFD5E5E2)
}

/** Primary action (save and analyze) — deep green on-brand. */
val MindoraSaveGreen = Color(0xFF1B5E3A)
